// Create a copy of this file named as config.ts using the format below.
export const config = {
    // Discord bot token
    token: "",
    // Client ID
    client_id: "",
    // Prefix for your bot
    prefix: "!sha-",
};

export default config;
